# Flux Go public website

This repository contains the static Flux Go landing page.

## Vercel

Import `saaro-org/saaro-website` into Vercel.

- Framework preset: Other
- Build command: none
- Output directory: `dist`

Add `fluxgo.in` as the custom domain in Vercel after the first deployment.
